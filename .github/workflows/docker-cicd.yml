name: Build and Push Docker Image - Users

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout do código
      uses: actions/checkout@v4

    - name: Login no Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build da imagem Docker
      run: |
        docker build \
          --build-arg GH_USERNAME=${{ secrets.GH_PACKAGES_USERNAME }} \
          --build-arg GH_TOKEN=${{ secrets.GH_PACKAGES_TOKEN }} \
          -t alqueiroz/users:latest \
          -t alqueiroz/users:1.0 .

    - name: Push da imagem no Docker Hub
      run: |
        docker push alqueiroz/users:latest
        docker push alqueiroz/users:1.0
